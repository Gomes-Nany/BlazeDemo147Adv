Instalação padrão  do playwright:
npm init playwright@test

Nosso projeto utilizará as seguintes abordagens:
- Separação dos cenários de negócio dos scripts de automação
- Vamos precisar de no mínimo 2 arquivos
 ---> Pasta features, que contem arquivos
    .Feature - Funcionalidades de cenários de negócio / critérios de aceitação  / Dados
 ---> Pasta steps, que contém arquivos
 .Spec.js - script de automação de teste
     instalações adicionais:
    // Instala o pacote que da suporte ao BDD
    // BDD = Desenvolvimento direcionado por comportamento
    npm install -D playwright-bdd
   
Feature: Login
   Scenario: Login with sucess
     Given
     When
     Then

Funcionalidade: Login
    Cenário: Login com sucesso
    Dado
    Quando
    Entao

Nomenclatura
- BDD - Behavior Driven Development - Desenvolvimento direcionado pelo comportamento
Abordagem de desenvolvimento, muito valorizado dentro da agilidade
Objetivo é facilitar a comunicação entre os membros do time, áreas e clientes.

- Gherkin: Instruções do BDD são escritas na sintaxe (Formato) chamado Gherkin

- cucumber/ Behavior/ Specflow são exemplos de plugins que fazem a integração entre arquivos .features
com arquivos .spec.js

- Page objects / Page Factory: Separar o mapeamento dos elementos da interface gráfica (páginas, telas) do código de automação
--> Pages com mapeamento de elementos
--> steps com os scripts de automação
--> Features com os cenários de teste

Em um projeto simples, cada teste é um arquivo .spec.js diferente
Em um projeto com Gherkin, cada teste é composto por um arquivo .Feature + um arquivo steps.js

dependencias removidas:
"@types/node": "^25.5.2",
"playwright-bdd": "^8.5.0"


diferenças de sintaxe conforme o type definido no packge.json

commonjs --> const ... = require('...')
module --> import ... from ...

Sobre execução sincrona (uma parte espera a outra / ex. do telefone) e 
Assincrona (a parte emissora não espera / ex. mensagem do whatsapp)

Sem async/await: transação sincrona
Com async/await: transação assincrona

Estamos usando dois frameworks:
- Playwright (controla o browser) --> Timeout (mensagem especifica que pode explicar a causa)
- Cucumber (organização)          --> Timeout (mensagem generica sem explicar a causa)

Situação da aula: Funcionava sem o await e não funcionava com o await
